// import * as Recipe from './Recipe'

// describe('Recipe', () => {
//   test('CRUD', async () => {
//     const recipeID = await Recipe.insert({
//       name: 'Puuro', time: 5
//     })

//     const recipe = await Recipe.find(recipeID)
//     expect(recipe.id).not.toBeUndefined()
//     expect(recipe.name).toEqual('Puuro')
//   })
// })
